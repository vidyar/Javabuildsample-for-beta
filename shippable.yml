language: java


script: 
   - ant coverage

matrix:
    include:
      - jdk: openjdk7
  #testing
after_archive:
      
      - echo $SHIPPABLE_ARCHIVE_DOWNLOAD_URL
      - echo $SHIPPABLE_ARTIFACTS_URL
      - echo $ARTIFACTS_UPLOAD_SUCCESSFUL
